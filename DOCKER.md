# 🐳 Docker инструкции

Этот документ содержит инструкции по запуску проекта в Docker контейнерах.

## 📋 Требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 1.29 или выше)

## 🚀 Быстрый старт

### 1. Подготовка

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
# Для SQLite в Docker используйте абсолютный путь (4 слеша!)
DATABASE_URL=sqlite:////app/data/bot_database.db
ADMINS=123456789,987654321
```

### 2. Запуск с SQLite (по умолчанию)

```bash
# Если у вас есть старая база данных (school_test.db), скопируйте её в volume:
# 1. Запустите контейнер временно
docker-compose up -d

# 2. Скопируйте старую базу в volume
docker cp school_test.db school_test_bot:/app/data/bot_database.db

# 3. Перезапустите контейнер
docker-compose restart

# Или если базы еще нет, просто запустите:
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
```

### 3. Запуск с PostgreSQL

1. Обновите `.env` файл:
```env
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/school_test
ADMINS=123456789,987654321
POSTGRES_DB=school_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
```

2. Запустите с PostgreSQL:
```bash
docker-compose -f docker-compose.postgres.yml up -d
```

## 📝 Полезные команды

### Просмотр логов
```bash
# Все сервисы
docker-compose logs -f

# Только бот
docker-compose logs -f bot

# PostgreSQL (если используется)
docker-compose logs -f postgres
```

### Остановка
```bash
docker-compose down
```

### Пересборка образа
```bash
docker-compose build --no-cache
docker-compose up -d
```

### Вход в контейнер
```bash
docker-compose exec bot bash
```

### Очистка данных
```bash
# Остановить и удалить контейнеры и volumes
docker-compose down -v
```

## 🔧 Настройка

### Изменение портов PostgreSQL

Если нужно изменить порт PostgreSQL, отредактируйте `docker-compose.postgres.yml`:

```yaml
postgres:
  ports:
    - "5433:5432"  # Внешний порт:Внутренний порт
```

### Использование внешней базы данных

Если у вас уже есть PostgreSQL сервер, просто укажите его URL в `.env`:

```env
DATABASE_URL=postgresql://user:password@host:5432/database
```

И используйте обычный `docker-compose.yml` (без PostgreSQL сервиса).

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи: `docker-compose logs bot`
2. Убедитесь, что `.env` файл существует и содержит все необходимые переменные
3. Проверьте, что BOT_TOKEN валидный

### Проблемы с базой данных

1. Для SQLite: 
   - Убедитесь, что директория `data` существует и доступна для записи
   - Используйте правильный формат пути: `sqlite:////app/data/bot_database.db` (4 слеша для абсолютного пути!)
   - Проверьте права доступа к директории: `chmod 755 data`
2. Для PostgreSQL: проверьте, что контейнер PostgreSQL запущен: `docker-compose ps`

### Очистка и перезапуск

```bash
# Полная очистка
docker-compose down -v
docker system prune -a

# Пересборка и запуск
docker-compose build --no-cache
docker-compose up -d
```

## 📦 Структура данных

- `./files` - файлы проекта (монтируются в контейнер)
- `./data` - данные SQLite базы (если используется)
- `postgres_data` - данные PostgreSQL (Docker volume, если используется)

## 🔐 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл в репозиторий
- Используйте сильные пароли для PostgreSQL в продакшене
- Ограничьте доступ к портам базы данных в продакшене

